//=============================================================================
// ■ channel_mute.cpp
//-----------------------------------------------------------------------------
//   静音的声音通道。
//=============================================================================

#include <VMDE.hpp>

namespace Audio {
	void Channel_Mute::fill(float* buf, unsigned long n) {
		while (n) {
			FEED_AUDIO_DATA(buf, .0f);
			n--;
		}
	}
}
